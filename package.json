{
  "name": "dealguard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx.cmd vite --host 127.0.0.1",
    "build": "vite build",
    "test": "node --test tests/*.test.js",
    "check": "node --check script.js && node --check src/dealguard-core.js && node --test tests/*.test.js"
  },
  "devDependencies": {
    "genlayer": "^0.39.2",
    "vite": "^7.1.5"
  },
  "dependencies": {
    "genlayer-js": "^1.1.8"
  }
}
